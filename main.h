#ifndef MAIN_H
#define MAIN_H
#include <string.h>
#include <unistd.h>
#include <stdarg.h>

void _puts(char *str);
void _putchar(int a);
int _printf(const char *format, ...);
int _strlen(const char *);
#endif
